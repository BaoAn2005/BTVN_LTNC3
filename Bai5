string hackerrankInString(string s) {
    const string t = "hackerrank";
    int p = 0;
    for (int i = 0; i < s.length(); i++) {
        if (s[i] == t[p]) p++;
        if (p == t.length() - 1) break;
    }
    if (p == t.length() - 1) return "YES";
    return "NO";
}
